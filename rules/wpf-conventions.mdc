---
description: WPF/XAML house conventions - apply when creating or editing XAML files.
globs: "**/*.xaml"
alwaysApply: false
---

Editing or characterizing WPF XAML - load `dotnet-wpf`: the FIRST action after this rule attaches is
that read, before the NEXT touch lands, edit or read-only (a path-scoped rule attaches ON the touch,
so it can never precede its own trigger, and a read-only characterization pass owes the load exactly
like an edit - measured: a code-style seat read XAML, never loaded it, then reported an unverified
'divergence from the house skill' claim) - skip the load when it is already in context (some seats
preload it); conventions are the source of truth, not recall. Name the skill you loaded, or say it
was already in context - the receipt is what makes the load happen.

Governs the `.xaml` view + MVVM binding layer; the `.cs` view-models are C#, governed by `csharp`.
Skip one-line tweaks.
